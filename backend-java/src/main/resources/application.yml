spring:
  application:
    name: ai-study-hub-java
  datasource:
    url: ${DB_URL:jdbc:sqlserver://${DB_HOST:localhost}:${DB_PORT:1433};databaseName=${DB_NAME:AI_StudyHub};encrypt=true;trustServerCertificate=true}
    username: ${DB_USERNAME:sa}
    password: ${DB_PASSWORD:YourStrong!Passw0rd}
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
server:
  port: ${SERVER_PORT:8080}
ai:
  python-service:
    base-url: ${PYTHON_AI_BASE_URL:http://localhost:8000}
