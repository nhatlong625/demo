# AI Study Hub Java Backend

Spring Boot service for business data and management APIs: users, courses, documents, quizzes, payments, roles, and admin workflows.

Place the Spring Boot project files here, including `pom.xml` and `src/`.
